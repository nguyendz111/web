<script>
    document.addEventListener('DOMContentLoaded', function() {let} urlParams = new URLSearchParams(window.location.search);
    let products = JSON.parse(urlParams.get('products'));

        // Xử lý dữ liệu products ở đây (ví dụ: hiển thị lên trang)
    });
</script>;
